#include <stdio.h>
#include <stdlib.h>

//C program to calculate the area of a sphere where A = 4ðr2

int main() 
{   float pie=3.14,r,area;
    r=scanf("%f",&r);
    area=4*pie*r*r;
    printf("%f is the area if give cirlce with radius %f\n",area,r);
	return 0;
}
